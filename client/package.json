{
  "name": "collab-editor-client",
  "version": "1.0.0",
  "description": "Frontend for collaborative code editor",
  "scripts": {
    "start": "live-server --port=8080",
    "build": "echo 'No build needed for simple HTML/JS project'"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}